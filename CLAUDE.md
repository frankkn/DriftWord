# DriftWord

## 產品核心

每天一個詞（如「消失」），用戶用語音（最多3段）或文字說出對這個詞的第一個記憶或感受。回應會漂流給一個陌生人，也會收到另一個陌生人的回應。沒有按讚、沒有追蹤，只有這一次交換。

這是一種儀式，不是社群 app。每個功能決策都要符合這個定調。

## 技術棧

- 前端：React + Tailwind
- 後端：Supabase（auth + db + storage）
- 語音：瀏覽器原生 MediaRecorder API
- 部署：Vercel

## 設計方向

極簡、沉浸、帶點詩意。

- 底色：接近紙張的米白（`#F5F0E8`）
- 文字：墨水感的深色（`#1A1A2E`）
- 主題色：暗藍或灰紫（`#4A5568` / `#6B5B9E`）
- 字型：Noto Serif TC（中文）+ serif 英文，內文要有閱讀感

## 協作方式

- 每次只說要做什麼功能，給完整可直接使用的程式碼
- 遇到需要決定的地方，先做合理選擇再說明原因
- 不解釋每一行，但關鍵決策要說
- 輸出完整檔案，不省略成 `// ...`
